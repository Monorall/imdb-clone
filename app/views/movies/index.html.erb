<% @movies.each do |movie| %>
  <section class="movie">
    <div class="summary">
      <h2>
        <%= link_to(movie.title, movie) %>
      </h2>
      <p>
        <%= truncate(movie.text, length: 150, separator: ' ') %>
      </p>
    </div>
  </section>
<% end %>